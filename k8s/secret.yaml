apiVersion: v1
kind: Secret
metadata:
  name: todo-app-secret
  labels:
    app: todo-app
type: Opaque
stringData:
  # In production, use kubectl create secret or sealed-secrets
  # stringData values are automatically base64 encoded by Kubernetes
  API_KEY: "todo-app-secret-key-2024"
  SECRET_TOKEN: "production-secret-token-123456"

