apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todo-app-pvc
  labels:
    app: todo-app
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard

